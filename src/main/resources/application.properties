# MongoDB Configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=notification

# Server Port
server.port=8080

# WebSocket Config
spring.websocket.path=/websocket-demo

